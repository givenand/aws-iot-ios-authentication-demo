# Uncomment the next line to define a global platform for your project
# platform :ios, '9.0'

target 'aws-iot-ios-authentication-demo' do
  # Comment the next line if you don't want to use dynamic frameworks
  use_frameworks!
  inhibit_all_warnings!
  pod 'AWSIoT'
  pod 'AWSCore'
  pod 'AWSMobileClient'

  # Pods for aws-iot-ios-authentication-demo

  target 'aws-iot-ios-authentication-demoTests' do
    inherit! :search_paths
    # Pods for testing
  end

  target 'aws-iot-ios-authentication-demoUITests' do
    # Pods for testing
  end

end
